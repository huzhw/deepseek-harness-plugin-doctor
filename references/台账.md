# DSH 插件台账与修复案例

> 数据基准：2026-09-12 盘点，2026-09-13 全量复核（插件升级体检），2026-09-12（Get-Date 口径）升级执行：dsh-permission-rules 0.7.1→0.7.2。桌面 app 0.12.2（09-12 实测，较 09-13 记录 0.12.0 已自更）/ dsh 引擎磁盘 dependencies\dsh\package.json 记 0.1.5-rc.1（与 09-13 记录 rc.2 有出入，见待办）/ Node 22.20.0 / pnpm 10.28.2。
> 路径速记：profile 根 = `C:\Users\Administrator\.dsh\profiles\`，日志 = `%APPDATA%\io.github.hairyf.deepseek-harness-desktop\logs\`，核心下载目录 = `%APPDATA%\io.github.hairyf.deepseek-harness-desktop\dependencies\`。

## 版本速查口径

- 引擎最新版：`npm view @deepseek-ai/dsh-web-app dist-tags` → 认 **next** 标签（latest 是 0.0.1-rc.1 占位假标签）
- 插件最新版：`npm view <包名> dist-tags`（latest 主通道，另有 legacy/beta）
- 桌面 app 版本：诊断导出「环境信息 app: x.y.z」或 desktop.log 启动行

## web profile 插件清单

来源：`profiles\web\package.json`（dependencies 27 条 + dsh.bundles 26 条，2026-09-12 复核；较 09-12 基准新增 @tt-a1i/archify-dsh、@vectorize-io/hindsight-coding-agents 两条，win-terminal-inspector 已移除见待办）

### @deepseek-ai 官方依赖（3 条，在位；引擎支撑库，零配置）

| 包 | 版本 | 干啥 | 配置 |
|---|---|---|---|
| @deepseek-ai/dsh-brand | 0.1.5-rc.1 | 无状态品牌原语类型（标识/命名基座） | 无需配置 |
| @deepseek-ai/dsh-credentials | 0.1.5-rc.1 | 凭证抽象层 ctx.credentials：settings 存引用、provider 持值 | 密钥落 `.credentials.yaml`，勿手改 |
| @deepseek-ai/dsh-invariants | 0.1.5-rc.1 | 运行时不变量注册服务 | 无需配置 |

bundles 里另引用 `@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app` 但 dependencies 无此二包 → `CORE_PLUGIN_PROFILE_ENTRY_MISSING` 常驻 WARN，不拦截启动，**别当病治**。

### 内部预设 dsh-tauri\*（10 个，app 启动自动补装）

| 插件 | 干啥 | 配置 |
|---|---|---|
| dsh-tauri | Tauri 壳基座：host 半层 + iframe 客户端消息桥 | 无需配置 |
| dsh-tauri-ui | 界面扩展：设置对话框改造为左侧停靠设置栏 | 无需配置 |
| dsh-tauri-worktree | 会话级 Git worktree 隔离（每会话独立目录，安全改码） | 无需配置 |
| dsh-tauri-panel | 面板能力扩展入口（注入点、内容区宽度拖拽） | 无需配置 |
| dsh-tauri-panel-extension | 面板内 Skills 与 MCP 管理 | 无需配置 |
| dsh-tauri-panel-scheduler | 面板「定时任务」能力 | 面板内管理 |
| dsh-tauri-session | 「已归档聊天」管理（搜索/排序/分组） | 设置内管理 |
| dsh-tauri-pet | 桌面宠物（开关/选宠物/置顶透明窗） | `~/.dsh\pet.json`（当前 petId: ouo-neko） |
| dsh-tauri-rightclick | 原生风格右键菜单（会话/工作区/文本） | 无需配置 |
| dsh-tauri-turnrewind | 回合级撤销：每回合 Git 快照+变更卡+一键回滚 | 自动快照，无需配置 |

- 来源全部是 `link:D:\tools\Deepseek Harness Desktop\resources\node_modules\<名字>`
- turnrewind 是 app 0.12.0 新增的第 10 个内部预设（2026-09-12 起）
- 补装成功标志：desktop.log `Preinstall plugins installed successfully`

### 社区/市场插件（14 个，2026-09-12 复核全部 npm latest / git HEAD 最新；win-terminal-inspector 已移除；按成熟度分两块）

> 五组导航（按功能）：🛡️**安全防护**（permission-rules / auto-review / defend）｜🗂️**管理效率**（dshmarket / mcp-panel / session-pin / better-sidebar）｜📊**观测计量**（cost-meter / context）｜🧩**能力扩展**（modlens / univer-office / hindsight / archify-dsh）｜🔧**兼容修复**（opencode-session）

#### ✅ 稳定成熟·公认首选（3 个，一般不用替换）

> 判据：1.0+ 正式版本线、npm 正规通道、无手工补丁史、无已知问题。

| 插件 | 版本/来源 | 干啥 | 配置/备注 |
|---|---|---|---|
| dshmarket | ^1.45.1 · ★3408 | 可视化插件市场：逛/搜/一键安装 | **桌面 app 出厂内置 + 注册表唯一 Recommended**；非官方出品（官方无指定市场），社区事实标准；**09-13 替代调研：不过时无需替换**（见下方调研注） |
| dsh-cost-meter | ^1.7.21 · ★262 / dl47k | 会话费用统计：本会话/当日/历史成本、90+ 模型价格、9 家 Coding Plan 额度、峰谷计价、中英双语 | 费用统计事实首选（Han-1413141 正主，有 ★0/★2 同名仿品勿装错）；「设置→插件」面板；账本自动存 `storages\cost-meter\ledger.json` |
| @liustack/modlens | ^3.26.1（3.x 成熟线）· ★3913 / dl166k | 给纯文本 LLM 加视觉（图片读取桥，免费 Antigravity CLI 驱动） | 无本地配置文件；`npx @liustack/modlens doctor` 自检 |

#### 🧪 轻量早期·需观察（12 个，0.x / git 源 / 有垫片史 / 依赖上游）

> 非"当前有问题"（14 个今日全部健康），也**不等于不安全**（安全三件套审计已通过，见下方审计注）——是"年轻/被打过补丁/依赖上游存亡/版本管理弱"，**升级时必看 changelog**。DSH 生态整体年轻（引擎才 0.1.5-rc），0.x 是常态。

| 插件 | 版本/来源 | 干啥 | 配置/备注 |
|---|---|---|---|
| dsh-permission-rules | 0.7.2（钉死，09-12 升）· ★113 | 声明式权限规则 allow/deny/ask + 进程级网络策略 | **0.7.1 曾发布当日带病需 4 处手改垫片（案例2）；09-12 升 0.7.2 实测上游已自修打包**（client.js 仅 require("react")，内置模块零外置，垫片退役）；升级仍必按案例2流程复查；**`~/.dsh\rules.yaml` 热重载**；`/rules list` 查生效规则 |
| dsh-auto-review | ^0.12.3 · ★146 | 第二模型 AI 自动审查审批请求（只读 reviewer 裁决 allow/deny，fail-closed，全审计） | 0.x 年轻线但安全性审计通过（官方件拼装、0 出站）；设置面板配审查模型；与 permission-rules 的 ask 联动 |
| dsh-defend | ^0.3.10 · ★7 | 提示注入/越狱/泄密检测拦截（Aho-Corasick 模式引擎，allow/ask/block） | 0.x；纯后端无 bundle 风险面；无独立配置文件；09-13 实证拦截 secret |
| dsh-context | ^0.50.0 · ★1328 / dl80k | 上下文洞察：context 仪表盘+命令，看上下文构成与演变 | 设置面板 |
| dsh-mcp-panel | ^0.6.14 · ★53 | MCP 管理台：/mcp 命令、健康诊断、设置 MCP 页 server 增删改 | GUI：设置→MCP 页 |
| dsh-session-pin | ^0.7.11 · ★7 | 会话/工作区置顶钉住 + 行色标记 | GUI：侧栏 [pin][色块] 控件 |
| dsh-univer-office | ^0.2.14 · ★298 / dl70k | DSH×Univer 办公集成：内联预览、浮动 Worktree 窗口、会话结束审查 | dream-num（Univer 官方公司 org）出品；捆绑 Gateway 的重集成件，早期版本线 |
| @tt-a1i/archify-dsh | ^0.1.0（09-13 补录）· 主项目 ★53756 | Archify 架构图 skill 的 DSH 打包（opt-in） | 早期版本；跟随 skill 使用 |
| dsh-opencode-session | ^0.1.1 · 注册表未收录 | 给 OpenCode provider 请求自动加 x-opencode-session 头（修 400 MissingSessionID） | 单一修复件：上游 OpenCode 官方修复此问题即退役；纯后端 |
| dsh-better-sidebar | github:omdsh-dev/DSH-better-sidebar · ★3421 / dl273k | VSCode 式右侧栏：资源管理器/编辑器/终端/git/浏览器，按会话隔离 | 0.x + git 源，但**生态基础设施**（第三方插件依赖它注册页签）且 tag 发布规范；0.19.1 = 最新正式 tag；有 N23 仿品勿装错 |
| @vectorize-io/hindsight-coding-agents | ^0.6.0 · 主项目 ★23210 | Hindsight 长期记忆：reflect 检索 + 后台自动摄取 | 0.x 但公司 org 维护，零配置自动运行 |
| dsh-builtin-browser | ^0.1.21（09-12 装）· ★51 / dl13k | 共享真实浏览器：可见原生 Electron 窗口，agent 走 CDP，20 个 browser_* 工具（打开/快照/执行/填表/截图/下载/登录态），任务级会话隔离+登录态持久化+人工接管 | **接替已退役的 chrome-devtools MCP**（同日退役，见待办终局结论）；`dsh plugin add dsh-builtin-browser` 安装，+13 包 |

> 通用规律：带 web-ui 的插件都有「设置→插件」面板配置项；host-only 的要么纯后台要么走专属文件。非插件配置文件：`settings.yaml` = LLM 供应商模型（settings-curator 技能管）、`dream-skin.json` + `skin-center-active.json` = 内置皮肤中心壁纸/皮肤。

> 🛡️ 安全审计（2026-09-13）：dsh-permission-rules 与 dsh-auto-review 的 `network` 出站声明均为 **0 条**，依赖全本地库（chokidar/yaml/zod）或 @deepseek-ai 官方件，安全可信赖。分工：permission-rules = 规则门禁（deny 硬拒 / ask 送审），auto-review = ask 的 AI 审批员（只读 reviewer + fail-closed + 全审计；审查调用走 settings.yaml 官方 LLM 通道，不私连第三方）；两者与 dsh-defend 构成「规则门禁 → AI 裁决 → 内容层拦截」三层安全链（defend 2026-09-13 实证拦截过一次 secret 输出）。小瑕疵：permission-rules 的 dependencies 混入 typescript/tsdown/@types/react 三个构建期依赖，打包瑕疵无碍安全。


### safe profile

安全模式专用精简 profile，无 permission-rules。进安全模式时 app 自动清用户插件（2026-09-12 清过 better-sidebar / win-terminal-inspector / dshmarket 三个，属正常清理不是事故）。

## 修复案例

### 案例1（2026-09-12）store 漂移 —— safe profile 起不来

- **症状**：DSH 启动失败，前台报 `INTERNAL_PLUGIN_INSTALL_FAILED: PREINSTALL_FAILED` + `ERR_PNPM_UNEXPECTED_STORE Unexpected store location`，指名 `profiles\safe`
- **根因**：用户级 `~\.npmrc` 加了 `store-dir=F:\Program Files (x86)\pnpm-store`（全局挪 F 盘），而 safe profile 的 `.npmrc` 没钉 store-dir；safe 的 `node_modules\.modules.yaml` 记录的是 `C:\Users\Administrator\AppData\Local\pnpm\store\v10`，pnpm 解析到 F 盘与记录不符即炸
- **背景**：web profile 同病已于 2026-09-10 修过（.npmrc 钉死），safe 漏钉
- **修法**：`profiles\safe\.npmrc` 补两行（与 web 同构）：`store-dir=C:\Users\Administrator\AppData\Local\pnpm\store` + `@deepseek-ai:registry=https://mirrors.cloud.tencent.com/npm/`
- **验证**：`pnpm -C safe store path` 回到 C 盘 v10；`pnpm -C safe install --frozen-lockfile` 478ms 零下载完成；重启后 `dsh-tauri-pet`、`dsh-tauri-turnrewind` 自动补装成功

### 案例2（2026-09-12）externals 漂移 —— web profile 页面 Failed to load plugins

- **症状**：进程活着、HTTP 正常、健康检查 74/74 通过（**假阳性**），浏览器渲染 "Failed to load plugins"，console：`failed to import loader entry 05381d36 (dsh-permission-rules): client-modules: require("process") missed the module table`
- **根因**：dsh-permission-rules 0.7.1（当天 10:12 发布、12:24 被 app 刷上）打包时把内置依赖 yaml@2.9.0 的 `process`、`buffer` 外置；dsh client-modules 运行时模块表无此种子。新旧核心运行时文件逐字一致 → 锅在插件构建，非核心回归
- **侦查法**：`npm pack dsh-permission-rules@0.6.21` 解包 grep 对比——0.6.21 的 client.js `require("process")` 为 0 处，0.7.1 为 3 处；0.6.21 peer 依赖声明兼容 `>=0.1.5-alpha.1`，回滚备选成立
- **修法**（`profiles\web\node_modules\dsh-permission-rules\lib\client.js` 共 4 处垫片，备份在 `profiles\web\.bak\dsh-permission-rules-0.7.1-client.js.bak`）：
  - 3 处 `require("process")` → `(globalThis.process ?? {env:{},emitWarning:function(){}})`（yaml 实际只用 `env.LOG_STREAM`/`env.LOG_TOKENS`/`emitWarning`，全覆盖）
  - 1 处 `var node_buffer = require("buffer");` → `var node_buffer = { Buffer: globalThis.Buffer };`（yaml binary 标签有 typeof 守卫，自动走 atob/btoa）
- **验证**：浏览器刷新后完整 UI 渲染、console 零报错（loader entry 哈希 05381d36 → ea876926 → 新值，证明服务端现读磁盘、补丁免重启生效）
- **残余风险**：插件下次升级覆盖垫片 → 重犯时先查新版是否自带修复，否则照方重打；`require("react")` 由宿主提供，不是病。**2026-09-12 升 0.7.2 复查：上游已自修打包（内置模块 require 计数=0），垫片退役无需重打**；09-12 15:36 重启 dsh 后 0.7.2 服务端代码加载生效，启动段无插件 ERROR

### 案例3（2026-09-13）端口竞态 EADDRINUSE —— 启动连崩两次后自愈

- **症状**：frontdesk.log 05:33:52 / 05:34:29 两次 `dsh process exited during startup`（exit code 1），desktop.log 对应 `listen EADDRINUSE: address already in use 127.0.0.1:13080`
- **根因**：旧 dsh 进程（10916）退出后 13080 短暂未释放，app 立刻拉起新进程（37552）listen 即撞；第二次重试又撞在残留占用上
- **自愈**：app 之后把 Harness 挪 13081 过渡（05:39-05:43），05:52 全停，05:56 端口释放后日志 `Harness port healed from 13081 back to 13080 (no longer occupied)`，pid 31784 稳定起在 13080
- **处置**：无需人工干预；若频繁复发再查是否有第三方进程抢占 13080

## 待办观察

- [x] dsh-permission-rules 升级后复查垫片是否被覆盖（2026-09-12 升 0.7.2：上游自修、垫片退役）；后续每次升级保持此复查
- [ ] dsh 引擎 rc.3 发布后确认 runtime 是否官方补 process/buffer 种子；若补，垫片可退役
- [ ] 桌面 app 新版本新增内部预设插件时（如 turnrewind 先例），首次启动会对每个 profile 各补装一次，属正常现象
- [x] chrome-devtools MCP 浏览器实例拉不起来（2026-09-13 体检：`Target setDiscoverTargets: Target closed` ×4；Chrome 装在 `%LOCALAPPDATA%\Google\Chrome` 用户级目录）。09-12 重启 app 后复测仍 Target closed（"重启让 MCP 重拉"假设证伪）→ **09-12 终局**：根因三层 = ①引擎 0.12.2 stdio 裸 spawn `npx` ENOENT（`.cmd` 垫片）②四端共用 Edge profile 单例互顶 ③引擎子进程环境里 puppeteer 带管道句柄的浏览器孵化无声失败（普通 spawn 正常、事件日志无崩溃、profile 目录零写入——排障证据链 `~\.dsh\.bak\` cdm-edge-probe.log / cdm-env-safe.txt）；**DSH 端退役 chrome-devtools MCP 改用 dsh-builtin-browser 插件**，sync-check 检查项14 改三端口径
- [ ] `profiles\node_modules`（profiles 根，非 web）存在一批断链 junction（react/immer/clsx 等旧结构遗留），不影响运行，择期清理
- [ ] git 源插件升级探路（2026-09-13 实测）：shell 的 git 可直连 GitHub（policy proxy 放行），`git ls-remote --tags --refs` 即可比版本；但作者可能打 tag 不 bump version 字段（win-terminal-inspector 先例），**以 lockfile 锁的 commit 对比远端 HEAD 为准**，version 字段会骗人
- [ ] 引擎版本账实出入（2026-09-12 发现）：台账 09-13 记引擎 rc.2，当日实测磁盘 `dependencies\dsh\package.json`=0.1.5-rc.1 且当日 12:32 被写过，桌面 app 已自更 0.12.0→0.12.2——疑 app 更新重置核心或 wrapper 包版本字段滞后，下次体检以运行时真实版本核实；09-12 15:36 重启后复核磁盘仍 rc.1
- [x] win-terminal-inspector 移除复盘入账（2026-09-12 复盘发现已不在 web profile：package.json 28→27 条、node_modules 无目录；非本次升级所为，09-13 后某时点被移除；社区插件基数 15→14）
- [ ] plugin-errors.json 历史账（2026-09-12 复核）：其 PREINSTALL_SILENT_FAIL 记录（win-terminal-inspector / dshmarket / better-sidebar 等名单，mtime 09-12 12:45 早于 15:36 重启）为 safe 模式清理期旧账，现 dshmarket / better-sidebar 均已在位，下次体检勿当新病
